# Semantic routing + CLR result cache + trajectory store.
# Enables embedding-based routing, semantic similarity search, and
# trajectory synthesis. Without these, the orchestrator falls back to
# keyword routing and exact-match caches.
# pip install -r requirements-embeddings.txt

sentence-transformers>=2.2,<3
scikit-learn>=1.3,<2
numpy>=1.24,<2
